import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Welcome" />

<div className="welcome-hero">
  <div className="welcome-hero-glow" />
  <div className="welcome-hero-top">
    <span className="welcome-eyebrow">Welcome to</span>
    <img className="welcome-logo" src="/mayov.svg" alt="May Interactive logo" />
  </div>
  <div className="welcome-title-wrap">
    <h1 className="welcome-title">May Interactive</h1>
    <p className="welcome-title-sub">Demos &amp; Stories</p>
  </div>
  <p className="welcome-subtitle">
    A hands-on lab where 3D graphics, shaders, and interface engineering collide — real-time
    WebGL scenes, GLSL experiments, and production UI, all built with React Three Fiber,
    Three.js, and WebAssembly.
  </p>
  <div className="welcome-badges">
    <span className="badge">React Three Fiber</span>
    <span className="badge">Three.js</span>
    <span className="badge">WebXR</span>
    <span className="badge">GLSL Shaders</span>
    <span className="badge">WASM / C++</span>
  </div>
</div>

---

## What's Inside

<div className="category-grid">
  <div className="category-card">
    <div className="category-icon">🧊</div>
    <h3>Three</h3>
    <p>
      React Three Fiber scenes — 3D products, terrain generation, shader experiments,
      post-processing, particles, and WebXR (VR) experiences.
    </p>
  </div>
  <div className="category-card">
    <div className="category-icon">🖥️</div>
    <h3>WebGL</h3>
    <p>
      Low-level WebGL experiments — custom shaders and rendering pipelines outside the R3F
      abstraction layer.
    </p>
  </div>
  <div className="category-card">
    <div className="category-icon">⚙️</div>
    <h3>C++</h3>
    <p>
      WebAssembly experiments compiled from C++ — real-time waveform generation rendered as 3D
      geometry.
    </p>
  </div>
  <div className="category-card">
    <div className="category-icon">🧩</div>
    <h3>Html / React</h3>
    <p>
      DaisyUI + Tailwind UI components — buttons, cards, badges, catalogues, and interactive panels.
    </p>
  </div>
</div>

---

## How to Use This Storybook

<div className="tutorial-steps">

<div className="step">
  <div className="step-number">1</div>
  <div className="step-body">
    <h4>Browse stories in the sidebar</h4>
    <p>
      Use the left panel to expand categories (<strong>Three</strong>, <strong>WebGL</strong>,{' '}
      <strong>C++</strong>, <strong>Html / React</strong>). Each folder contains a component with
      one or more story variants. Click any story name to open it.
    </p>
  </div>
</div>

<div className="step">
  <div className="step-number">2</div>
  <div className="step-body">
    <h4>Interact with Controls</h4>
    <p>
      Open the <strong>Controls</strong> panel at the bottom of the screen. Stories that expose
      Leva or args-based controls let you tweak parameters (colors, sizes, shader uniforms,
      speeds) in real time without touching code.
    </p>
    <div className="tip">
      💡 Not all stories have controls — if the panel is empty, the story is self-contained. Press{' '}
      <kbd>Alt+A</kbd> to toggle the addons panel and reveal the Controls tab.
    </div>
  </div>
</div>

<div className="step">
  <div className="step-number">3</div>
  <div className="step-body">
    <h4>Resize the viewport</h4>
    <p>
      Use the <strong>screen 📱</strong> icon in the toolbar to preview stories at common
      breakpoints — mobile, tablet, desktop — and custom sizes like Instagram Post (4:5) or
      Instagram Square (1:1).
    </p>
  </div>
</div>

<div className="step">
  <div className="step-number">4</div>
  <div className="step-body">
    <h4>Keyboard shortcuts</h4>
    <div className="shortcuts-grid">
      <div className="shortcut">
        <kbd>Alt+S</kbd>
        <span>Toggle sidebar</span>
      </div>
      <div className="shortcut">
        <kbd>Alt+A</kbd>
        <span>Toggle addons panel</span>
      </div>
      <div className="shortcut">
        <kbd>Alt+F</kbd>
        <span>Fullscreen</span>
      </div>
      <div className="shortcut">
        <kbd>Alt+→</kbd>
        <span>Next story</span>
      </div>
      <div className="shortcut">
        <kbd>Alt+←</kbd>
        <span>Previous story</span>
      </div>
    </div>
  </div>
</div>

</div>

---

<div className="welcome-footer">
  <p>
    Built by <strong>David Mayorga Herrera</strong> ·{' '}
    <a href="https://mayinteractive.io" target="_blank">
      mayinteractive.io
    </a>
  </p>
</div>

<style>
  {`
  /* ── Hero ─────────────────────────────── */
  .welcome-hero {
    position: relative;
    background: linear-gradient(160deg, #1a1015 0%, #1e1e1e 55%, #150a10 100%);
    border: 1px solid #3a2430;
    border-radius: 12px;
    padding: 48px 56px 56px;
    margin-bottom: 48px;
    overflow: hidden;
  }

  .welcome-hero-glow {
    position: absolute;
    top: -180px;
    right: -140px;
    width: 420px;
    height: 420px;
    background: radial-gradient(circle, #8f305655 0%, transparent 70%);
    pointer-events: none;
  }

  .welcome-hero-top {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 28px;
  }

  .welcome-logo {
    width: 40px;
    height: 40px;
    flex-shrink: 0;
  }

  .welcome-eyebrow {
    display: inline-block;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: #d68dab;
  }

  .welcome-title-wrap {
    position: relative;
    text-align: center;
    padding: 16px 0 28px;
    margin: 0 auto 32px;
    max-width: 900px;
  }

  .welcome-title {
    font-size: 4.5rem;
    font-weight: 800;
    line-height: 1.05;
    color: #c26e94;
    margin: 0;
    letter-spacing: -1px;
    text-transform: uppercase;
  }

  .welcome-title-sub {
    font-size: 1.4rem;
    font-weight: 400;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: #d68dab;
    margin: 18px auto 0;
    padding-top: 18px;
    max-width: 480px;
    border-top: 1px solid #8f3056;
  }

  .welcome-subtitle {
    position: relative;
    text-align: center;
    font-size: 1.15rem;
    color: #cccccc;
    line-height: 1.7;
    margin: 0 auto 32px;
    max-width: 620px;
  }

  .welcome-badges {
    position: relative;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 8px;
  }

  .badge {
    background: #8f305622;
    border: 1px solid #8f3056;
    color: #d68dab;
    border-radius: 4px;
    padding: 4px 12px;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.3px;
  }

  /* ── Category Grid ─────────────────────── */
  .category-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin: 32px 0 48px;
  }

  .category-card {
    background: #1a1a1a;
    border: 1px solid #333;
    border-radius: 6px;
    padding: 28px 24px;
    transition: border-color 0.2s;
  }

  .category-card:hover {
    border-color: #8f3056;
  }

  .category-icon {
    font-size: 2rem;
    margin-bottom: 12px;
  }

  .category-card h3 {
    color: #ffffff;
    font-size: 1.1rem;
    font-weight: 700;
    margin: 0 0 8px;
  }

  .category-card p {
    color: #aaaaaa;
    font-size: 0.9rem;
    line-height: 1.6;
    margin: 0;
  }

  /* ── Tutorial Steps ────────────────────── */
  .tutorial-steps {
    display: flex;
    flex-direction: column;
    gap: 32px;
    margin: 32px 0 48px;
  }

  .step {
    display: flex;
    gap: 20px;
    align-items: flex-start;
  }

  .step-number {
    width: 36px;
    height: 36px;
    min-width: 36px;
    border-radius: 50%;
    background: #8f3056;
    color: #ffffff;
    font-weight: 700;
    font-size: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 2px;
  }

  .step-body {
    flex: 1;
  }

  .step-body h4 {
    color: #ffffff;
    font-size: 1.05rem;
    font-weight: 700;
    margin: 0 0 8px;
    padding: 0;
  }

  .step-body p {
    color: #cccccc;
    font-size: 0.9rem;
    line-height: 1.7;
    margin: 0 0 10px;
  }

  .step-body ul {
    color: #cccccc;
    font-size: 0.9rem;
    line-height: 1.7;
    margin: 8px 0 10px 18px;
    padding: 0;
  }

  .step-body ul li {
    margin-bottom: 4px;
  }

  .tip {
    background: #1a1a1a;
    border-left: 3px solid #8f3056;
    border-radius: 0 4px 4px 0;
    padding: 10px 14px;
    font-size: 0.85rem;
    color: #bbbbbb;
    margin-top: 10px;
  }

  /* ── Keyboard shortcuts ─────────────────── */
  .shortcuts-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 10px 24px;
    margin-top: 10px;
  }

  .shortcut {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.88rem;
    color: #cccccc;
  }

  kbd {
    background: #2a2a2a;
    border: 1px solid #555;
    border-radius: 4px;
    padding: 2px 8px;
    font-family: monospace;
    font-size: 0.8rem;
    color: #d68dab;
    white-space: nowrap;
  }

  code {
    background: #2a2a2a;
    border-radius: 3px;
    padding: 1px 6px;
    font-family: monospace;
    font-size: 0.85rem;
    color: #d68dab;
  }

  /* ── Footer ─────────────────────────────── */
  .welcome-footer {
    text-align: center;
    padding: 24px 0;
    color: #999999;
    font-size: 0.9rem;
  }

  .welcome-footer strong {
    color: #cccccc;
  }

  .welcome-footer a {
    color: #d68dab;
    text-decoration: none;
  }

  .welcome-footer a:hover {
    text-decoration: underline;
  }

  /* ── Responsive ─────────────────────────── */
  @media screen and (max-width: 700px) {
    .welcome-hero {
      padding: 28px 24px 36px;
    }
    .welcome-logo {
      width: 30px;
      height: 30px;
    }
    .welcome-title {
      font-size: 2.5rem;
      letter-spacing: -0.5px;
    }
    .welcome-title-sub {
      font-size: 1rem;
      letter-spacing: 2px;
    }
    .category-grid {
      grid-template-columns: 1fr;
    }
    .shortcuts-grid {
      grid-template-columns: 1fr;
    }
  }
`}
</style>
